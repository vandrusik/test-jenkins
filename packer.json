{
  "variables": {
    "git_commit": "",
    "git_branch": "",
    "project_id": "null"
  },
  "builders": [
    {
      "type": "googlecompute",
      "project_id": "{{user 'project_id'}}",
      "source_image": "ubuntu-1404-trusty-v20151113",
      "zone": "us-central1-a",
      "image_name": "test—jenkins-{{timestamp}}-{{user 'git_branch'}}-{{user 'git_commit'}}"
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "scripts": [
        "postinstall.sh"
      ]
    }
  ]
}
